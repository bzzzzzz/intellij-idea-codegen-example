plugins {
    id 'java-library'
    id 'idea'
}

repositories {
    mavenCentral()
}

dependencies {
    // This dependency is exported to consumers, that is to say found on their compile classpath.
    api project(path: ":lib", configuration: "generated")
}
